# Changesets

This directory stores release notes for publishable package changes.

- Add one file per release-worthy change.
- Use `npx changeset` (or `npm run changeset`) to create entries.
- `changeset version` updates package versions and changelogs.
- `changeset publish` publishes packages to npm.
