# Codex CLI interface metadata (optional).
#
# This file is safe to include even if a client ignores it.
# Icons must be relative paths under ./assets/.
[interface]
display_name = "RemNote"
short_description = "支持 Host API 远端模式与 current/compact 的 RemNote 安全写入技能"
# icon_small = "./assets/small-400px.png"
# icon_large = "./assets/large-logo.svg"
# brand_color = "#2563EB"
# default_prompt = "Optional surrounding prompt to use the skill with"
