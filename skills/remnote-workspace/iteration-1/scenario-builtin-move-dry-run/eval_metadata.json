{
  "eval_id": 16,
  "eval_name": "scenario-builtin-move-dry-run",
  "prompt": "我记不清 `dn_recent_todos_to_today_move` 这条 builtin scenario 的 var 名和默认时间范围了。先别真的执行，给我最稳的检查顺序和最终 dry-run 命令。我想把过去 3 天的 DN todo 搬到今天。",
  "assertions": [
    "Mentions a discovery flow such as `scenario builtin list`, `scenario builtin install`, or `scenario schema explain` before final execution",
    "Uses `source_scope=daily:past-3d`",
    "Uses `target_ref=daily:today`",
    "Uses `--dry-run` before any real write",
    "Does not use the retired `daily:previous-*` token"
  ]
}
