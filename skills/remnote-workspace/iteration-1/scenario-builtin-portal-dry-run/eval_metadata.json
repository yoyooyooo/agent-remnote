{
  "eval_id": 18,
  "eval_name": "scenario-builtin-portal-dry-run",
  "prompt": "我想把过去 2 天 DN 里的 todo 先以 portal 方式汇总到今天，但先别真的写。builtin id 和时间范围我都记不清了。给我最稳的检查顺序和 dry-run 命令。",
  "assertions": [
    "Uses the portal variant rather than the move variant",
    "Uses a discovery step such as `scenario builtin list` or `scenario schema explain`",
    "Uses `source_scope=daily:past-2d`",
    "Uses `target_ref=daily:today`",
    "Uses `--dry-run` before any real write"
  ]
}
