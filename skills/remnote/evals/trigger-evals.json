[
  {
    "query": "我现在就在 RemNote 桌面端里，刚选中了一个标题 rem。下面已经有几条乱掉的子项，你别新建页面，也别往页面根下插新节点，直接把这个标题下面的 children 重写成一个更清楚、由浅入深的大纲，标题本身要保留。",
    "should_trigger": true
  },
  {
    "query": "我在容器里跑 agent-remnote，`apiBaseUrl` 已经指到宿主机了。帮我把 rem id `4MZL7Gxd3MIwn3M9J` 下面追加一段结构化 Markdown，不要碰容器里的本地 `remnote.db`，也不用 wait。",
    "should_trigger": true
  },
  {
    "query": "把这段调研总结整理后写进今天的 RemNote Daily Note，要求只有一个主线入口，后面还能继续往下展开。不要为了看起来安全再包一层多余容器，也别先全库搜索。",
    "should_trigger": true
  },
  {
    "query": "agent-remnote 刚返回 `sent=0`，而且我下一步还要继续依赖这次写入结果。你先别猜，帮我给出最短排障顺序：先看 daemon、plugin，还是 queue wait，怎么确认是哪个环节没接上。",
    "should_trigger": true
  },
  {
    "query": "我接下来要继续在 RemNote 当前页面里写东西，但先想知道当前 page、focus rem 和 selection 大概是什么。给我最低 token 的读取路径，不要一上来就 search 全库。",
    "should_trigger": true
  },
  {
    "query": "我有个 `~/.agent-remnote/scenarios/recent-review.json`，先想知道它到底暴露了哪些 vars 和默认值，再决定要不要 run。现在在 `apiBaseUrl` 模式，先别真的写入。",
    "should_trigger": true
  },
  {
    "query": "我记不清 `dn_recent_todos_to_today_move` 这条 builtin scenario 的 `source_scope` 怎么传了。想先 dry-run 看 lowering，目标是把过去 3 天 DN 里的 todo 搬到今天，而且今天不算进 source。",
    "should_trigger": true
  },
  {
    "query": "我已经有一个 RemNote 的 rem id，想把它沉淀成独立 page，并且在原位置留一个 portal。别走那种先写 DN、再手工 portal create、再手工 move 的旧多步路径，帮我选更稳的 route。",
    "should_trigger": true
  },
  {
    "query": "RemNote 里有个 section，我只想把它下面的 direct children 全部清空，这个 section 自己必须保留，不能把整棵树删掉。给我最短正确路径，不要做多余读取。",
    "should_trigger": true
  },
  {
    "query": "RemNote 里一个已经在 UI 里配置好的 `single_select` 列，我现在在远端 `apiBaseUrl` 模式下想给它加一个 option。先判断这个命令支不支持透明远程执行，别把 generic property type mutation 和 option add/remove 混在一起。",
    "should_trigger": true
  },
  {
    "query": "帮我把 Notion 这个页面下面所有子 block 全部替换成新的 Markdown 内容，并同步回数据库；如果要先查当前 block 树也没关系。",
    "should_trigger": false
  },
  {
    "query": "我想在 Obsidian 的今天日记里追加一段模板内容，最好顺手帮我把旧段落归档一下，别碰 RemNote。",
    "should_trigger": false
  },
  {
    "query": "现在只想知道 Arc 当前 page 和 focus tab 是什么，越短越好，后面我要继续整理浏览器窗口。",
    "should_trigger": false
  },
  {
    "query": "BullMQ 这边一个 job 卡住了，我现在是在 worker 容器里。你觉得先看 queue wait 还是先看 worker logs，更容易判断是 lease 还是 handler 卡死？",
    "should_trigger": false
  },
  {
    "query": "给我一个 apply payload，把这几个 GitHub issue 一次性批量打上标签、发评论，再顺手改成 closed。",
    "should_trigger": false
  },
  {
    "query": "把这条飞书文档下面新增两条子项，顺便置顶插入一个总结块，我后面还会继续改这篇文档的结构。",
    "should_trigger": false
  },
  {
    "query": "我想直接改 SQLite 里的 `notes` 表，把几条记录的 `title` 批量替换掉，然后再补一个更新时间字段。",
    "should_trigger": false
  },
  {
    "query": "帮我设计一个 GitHub Actions 用的 scenario package，我也想在 JSON 里放 `source_scope` 和 `target_ref` 这种字段名，不过这和 RemNote 没关系。",
    "should_trigger": false
  },
  {
    "query": "请帮我做一个 tmux 状态栏 segment，显示当前页面标题和焦点 pane 信息，后面我想接进自己的 `.tmux.conf`。",
    "should_trigger": false
  },
  {
    "query": "React 里这个树节点的 children 要先清空再重新渲染，帮我写一个最短的组件方案，必要的话可以直接重构 state shape。",
    "should_trigger": false
  }
]
